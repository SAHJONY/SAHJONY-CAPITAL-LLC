# Sahjony CRM API Draft

This folder will hold Supabase Edge Functions (or other backend logic) once we wire them up.
Current TODOs:
1. Use Supabase schema from `../schema.sql`.
2. Add functions for metrics ingestion (Bland.ai, Stripe, etc.).
3. Build client APIs for the dashboard.
