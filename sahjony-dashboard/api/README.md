# Sahjony Dashboard API

This folder is for future backend endpoints or Supabase Edge Functions that feed data into the dashboard.
Current plan:
- Build a `dashboard_metrics` view in Supabase.
- Expose a lightweight JSON endpoint consumed by the Vercel site.
